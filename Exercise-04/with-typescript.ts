// Write a function greet that takes in a name (string) and returns a greeting (string).

function greet(name: string): string {
    return `Hello, ${name}!`;
}

const greeting: string = greet("Amanda");
console.log(greeting);